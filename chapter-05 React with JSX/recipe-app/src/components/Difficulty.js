import React from 'react'

const Difficulty = ({ difficulte }) =>
    <section className="difficulty">
        <h2>Difficulté</h2>
        <p>{difficulte}</p>
    </section>

Difficulty.displayName = 'Difficulté'

export default Difficulty